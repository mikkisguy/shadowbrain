import { ComingSoonPage } from "@/components/layout/coming-soon-page";

export default function ChatPage() {
  return (
    <ComingSoonPage
      label="Chat"
      description="A grounded chat interface over your second brain — ask questions, surface linked notes, and explore ideas in context."
    />
  );
}
