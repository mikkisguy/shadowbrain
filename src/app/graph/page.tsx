import { ComingSoonPage } from "@/components/layout/coming-soon-page";

export default function GraphPage() {
  return (
    <ComingSoonPage
      label="Graph"
      description="A visual knowledge graph over your notes, links, and tags. The placeholder marks the route so the navigation graph is intact; the visual is a Phase 5 deliverable."
    />
  );
}
